# 统一换行符，避免 Windows 上开发、Linux 上部署时出现 ^M 问题
#
# 背景：在 Windows 上 clone/编辑后，Git 默认会把 .sh 里的 LF 转成 CRLF，
# 推到服务器上执行会报 `bad interpreter: /bin/bash^M: no such file or directory`。
# 这里强制脚本类文件在仓库里和检出后都保持 LF。

# 默认：文本文件自动识别，仓库内统一存 LF
* text=auto eol=lf

# ─── Shell / 服务脚本：必须 LF ───
*.sh          text eol=lf
*.service     text eol=lf
*.conf        text eol=lf
Dockerfile    text eol=lf

# ─── Python ───
*.py          text eol=lf
*.pyi         text eol=lf

# ─── 配置 / 文档 ───
*.md          text eol=lf
*.txt         text eol=lf
*.ini         text eol=lf
*.cfg         text eol=lf
*.toml          text eol=lf
*.yml         text eol=lf
*.yaml        text eol=lf
*.json        text eol=lf
*.mako        text eol=lf

# ─── Windows 批处理：必须 CRLF ───
*.bat         text eol=crlf
*.cmd         text eol=crlf
*.ps1         text eol=crlf

# ─── 二进制：不做任何转换 ───
*.png         binary
*.jpg         binary
*.jpeg        binary
*.gif         binary
*.ico         binary
*.svg         text eol=lf
*.woff        binary
*.woff2       binary
*.ttf         binary
*.onnx        binary
*.exe         binary
*.zip         binary
*.db          binary
*.joblib      binary

# ─── 语言统计：不把这些算进代码量 ───
docs/**       linguist-documentation
*.md          linguist-documentation
